CREATE TABLE job_applied(
    jon_id int,
    application_sent_date date,
    custin_resume boolean,
    resume_file_name varchar(255),
    cover_letter_sent boolean,
    cover_letter_file_name varchar(255),
    status varchar(50)
);



